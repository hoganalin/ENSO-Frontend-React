# ============================================
# Supabase 配置
# ============================================
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# ============================================
# ⚠️ 金流 / 簡訊 / 發票的金鑰「不」放在這裡
# ============================================
# 凡是 VITE_ 前綴的變數都會被 Vite 打包進瀏覽器 bundle，
# 任何人打開 devtools 就能拿到。ECPay 的 HashKey 一旦外流，
# 就能自行簽章偽造訂單金額（把 3000 元的訂單簽成 1 元）。
# 這不是優化項，是安全紅線。
#
# 因此以下變數已全部移除，改用 Supabase secrets（只存在伺服器端）：
#
#   VITE_ECPAY_MERCHANT_ID    → ECPAY_MERCHANT_ID
#   VITE_ECPAY_HASH_KEY       → ECPAY_HASH_KEY
#   VITE_ECPAY_HASH_IV        → ECPAY_HASH_IV
#   VITE_ECPAY_IS_PRODUCTION  → ECPAY_IS_PRODUCTION
#   VITE_ECPAY_RETURN_URL     → ECPAY_RETURN_URL
#                               （語意也變了：現在是指向 payment-notify 的
#                                 伺服器端背景回調網址，不是給使用者看的頁面）
#   VITE_ECPAY_NOTIFY_URL     → 已刪除。綠界 AIO CheckOut 根本沒有 NotifyURL
#                               這個參數，背景回調用的是 ReturnURL。
#   VITE_MITAKE_USERNAME      → MITAKE_USERNAME
#   VITE_MITAKE_PASSWORD      → MITAKE_PASSWORD
#
# 設定方式（在專案根目錄執行）：
#   supabase secrets set ECPAY_MERCHANT_ID=2000132
#   supabase secrets set ECPAY_HASH_KEY=...
#   supabase secrets set ECPAY_HASH_IV=...
#
# 完整清單、本機開發方式、以及「人類必須自己去綠界／三竹後台設定什麼」，
# 見 supabase/functions/README.md。
#
# 如果你的 .env.local 裡還留著上面那些 VITE_ 變數，請直接刪掉 ——
# 留著不會有任何效果，只會讓下一個人以為金鑰該放在前端。
