# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment (keep .env.example tracked, everything else out)
.env
.env.*
!.env.example

# Build outputs
*.tsbuildinfo
tmp/

# Vercel CLI metadata (project-id, environment links)
.vercel

# IDE / Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Windows NTFS alternate data streams (從外網下載的檔案殘留)
*:Zone.Identifier

# Claude / AI tooling local overrides
.claude/settings.local.json

# Test / Coverage
coverage/
.nyc_output/

# Optional cache directories
.npm
.eslintcache

.vercel
.env*.local

# 本 session 的暫存／備份檔（可整個資料夾刪除）
_to_delete/
